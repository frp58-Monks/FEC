import React from 'react';

const ImageCarousel = (props) => {
  console.log({'ImgCarousel Styles Prop': props.productStyles});
  return (
    <div>images carousel here baby</div>
  );
}

export default ImageCarousel;